---
description: 운영/스테이징 배포 — FTP·JAR·VPS 재시작 (로컬 PG START와 별개)
alwaysApply: false
---

# 배포 규칙 (운영 서버 반영 시)

> **로컬 PC에서 개발**을 마친 뒤 **운영(또는 스테이징)에 올릴 때**만 적용한다.  
> 로컬에서 `PG START` 로 재시작하는 절차는 **`development-workflow.mdc`** — 여기서는 다루지 않는다.

## FTP 업로드 vs 데몬(백엔드) 재시작

| 변경 대상 | 업로드 | 데몬 재시작 |
|-----------|--------|-------------|
| **site/** (HTML, JS, CSS) | ✅ 필요 | ❌ 불필요 |
| **pg-app/** Java 소스 반영 | JAR 빌드 후 업로드 | ✅ **필요** |
| **pg-app/** 설정만 (`application.yml` 등) | ✅ 업로드 | ✅ **필요** |

상세: [docs/배포_가이드_FTP_업로드_및_재시작.md](../docs/배포_가이드_FTP_업로드_및_재시작.md)

## 배포 요청 시 필수 제공 (AI·문서 공통)

**배포**를 할 때 **반드시** 다음을 제공한다.

### 1. 업로드할 파일 목록 (경로 + 파일명)
- 예: `site/js/config.js`, `site/index.html`, `pg-app/build/libs/pg-app-0.0.1-SNAPSHOT.jar`

### 2. 변경 요약
- 파일별로 무엇이 바뀌었는지 한 줄 이상.

### 3. DB
- 운영 DB에 실행할 SQL 경로 또는 내용, 없으면 **「DB 변경 없음」**.

### 4. 원격 데몬 재시작 (VPS)
- **재시작 필요** 또는 **재시작 불필요** 를 명확히 적는다.  
- **필요 시**: 서버에서 `./restart-pg-app.sh` 등 (서버_재시작_방법.md, 배포 가이드 참고).  
- **로컬 `PG START`는 배포 절차가 아님.**

### 5. 확인 링크
- 실제 도메인으로: 예) 관리자 [https://icopay.co.kr/index.html](https://icopay.co.kr/index.html) (운영 URL이 다르면 그에 맞게)

## 배포 요청 시
- 위 1~5를 한 번에 정리해 제공한다.
