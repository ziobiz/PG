# PG 통합관리자 – 기능 구현 개발 방안

## 1. 결론: **개발 가능합니다**

운영 중인 실제 사이트 소스가 없어도, 지금 갖고 계신 것만으로 **기능 개발을 진행할 수 있습니다.**  
아래처럼 **참고 소스(PG소스_CD) + 분석 보고서 + 이미 만든 UI**를 조합해, 단계적으로 백엔드 API를 만들고 화면과 연결하면 됩니다.

---

## 2. 현재 보유 자산 정리

| 보유 자료 | 용도 |
|-----------|------|
| **PG소스_CD** | 레거시 로직·DB·URL 구조 참고. JSP/Spring MVC(`*.do`), PGMATE(DAO/Bean), Oracle 스키마 등 |
| **PG_분석보고서.md** | DB 테이블 역할, 로그인·결제 플로우, 메뉴/역할 매핑. API·화면 설계 시 기준 |
| **site/** (현재 UI) | 메뉴·탭·화면(검색/폼/테이블/페이징) 이미 구현됨. **실데이터만 붙이면 됨** |
| **pg-app/** | Spring Boot 골격만 있음. 여기에 API·서비스·DB 연동 구현 예정 |

운영 사이트 소스가 없어도, **“참고 소스로 스펙을 복원 → 새 백엔드로 재구현”** 방식으로 진행하면 됩니다.

---

## 3. 개발 전략 (3단계)

### 3.1 1단계: API 스펙 정하기

- **화면별로** “이 화면이 필요로 하는 데이터”를 정의합니다.
  - 예: 공지사항 목록 → `GET /api/system/notice?title=…&from=…&to=…&page=1&size=20`
  - 예: 결제내역 목록 → `GET /api/calc/payList?…`
  - 예: 업체 등록 → `POST /api/comp/register`
- 참고할 것:
  - **PG_분석보고서.md**의 메뉴·DB 테이블·플로우
  - **PG소스_CD**의 `sjmir-servlet.xml` (`/trnsctn.do`, `/webpay.do` 등 URL) 및 해당 Controller/DAO
  - **site/js/screens.js**의 `MENU_SCREENS` (검색 필드·컬럼명) → API 요청 파라미터·응답 필드와 맞춤

결과물: **화면–API 매핑표** (URL, 요청/응답 예시, 사용 DB 테이블).

### 3.2 2단계: 백엔드 구현 (pg-app)

- **pg-app**(Spring Boot)에서 위 스펙대로 REST API를 구현합니다.
- 참고 소스 활용:
  - **PG소스_CD**의 `SJMIR_APP`(Controller), `PGMATE`(DAO/Bean) 로직을 **참고**해, 동일 비즈니스 규칙을 Spring 방식으로 다시 작성
  - **DB_Oracle10g** 테이블 정의 → Oracle 유지하거나, 필요 시 MySQL/PostgreSQL 스키마로 옮겨서 사용
- DB가 아직 없으면:
  - 분석 보고서의 테이블 구조를 기준으로 **임시 스키마 + 더미 데이터**로 먼저 구현한 뒤, 실제 DB가 정해지면 연결만 바꿔도 됩니다.

한 번에 전 메뉴를 만들 필요 없이, **우선 1~2개 화면(예: 공지 목록, 결제내역 목록)**만 실데이터 연동부터 하는 것을 권장합니다.

### 3.3 3단계: UI와 API 연동 (site)

- **site**는 이미 메뉴·화면이 있으므로, “데이터만 API에서 가져오기”만 하면 됩니다.
- `config.js`의 `PG_BASE_URL`(또는 API 전용 base URL)에 **pg-app** 주소를 두고,
- 각 화면의 검색/조회/저장 버튼에서:
  - `fetch` 또는 `axios`로 위에서 정한 API 호출
  - 응답으로 테이블 데이터 갱신, 페이징, 저장 후 메시지 등 처리
- 지금 있는 `TODO: API 연동` 위치들(예: 업체 등록 저장)을 위 API 호출로 교체하면 됩니다.

이렇게 하면 **모든 페이지**를 같은 방식으로 점진적으로 연동할 수 있습니다.

---

## 4. “참고만 가능”인 점과 그에 대한 대응

| 우려 | 대응 |
|------|------|
| 운영 사이트 소스 없음 | PG소스_CD + 분석보고서로 **스펙·로직 복원** 후, pg-app에서 **새로 구현**. 실서비스와 필드/규칙이 다르면 그때 스펙만 수정 |
| PG소스_CD는 오래된 기술 스택 | **재구현**이 목표. Controller/DAO의 **역할·입출력**만 참고하고, 코드는 Spring Boot + REST + 현재 DB에 맞게 새로 작성 |
| 메뉴/화면이 참고 사이트와 다름 | 이미 만든 **site** 메뉴/화면을 기준으로, **그에 맞는 API**만 설계하면 됨. 참고 사이트는 “기능 목록·필드 의미” 참고용 |
| DB가 없거나 다름 | 분석 보고서·PG소스_CD 스키마로 **테이블 설계** 후, 개발용 DB 구축 또는 더미 JSON으로 먼저 연동 후, 나중에 실제 DB로 교체 |

---

## 5. 권장 진행 순서

1. **공통**
   - pg-app에 API 기본 설정 (CORS, 인증 체크, 예외 처리).
   - 로그인/세션(또는 토큰) 정책 결정 후, site와 pg-app에 적용.

2. **1~2개 화면으로 검증**
   - 예: **공지사항 목록** + **결제내역 목록**
   - API 스펙 작성 → pg-app에 해당 API 구현(더미/실DB) → site에서 호출해 테이블·페이징 연동.
   - 이 단계가 되면 “실제로 개발 가능하다”가 확인됩니다.

3. **나머지 메뉴 확대**
   - 화면–API 매핑표 기준으로, 업체관리·수수료·정산·기타 메뉴 순서대로 API 추가 + site 연동.

4. **결제/정산 등 핵심 로직**
   - PG소스_CD의 결제·취소·정산 로직을 참고해, pg-app에서 서비스 계층으로 재구현 후, 해당 API와 화면 연결.

---

## 6. 정리

- **개발은 할 수 있습니다.**  
  운영 사이트 소스가 없어도, **PG소스_CD(참고) + 분석보고서(스펙·DB) + 현재 UI(site)** 조합으로 진행 가능합니다.
- **방식:**  
  참고 소스로 **API 스펙·비즈니스 규칙**을 정하고, **pg-app에서 새 백엔드를 구현**한 뒤, **이미 만든 UI(site)**에 연동합니다.
- **리스크 완화:**  
  먼저 **1~2개 화면만** 실데이터 연동해 보신 뒤, 그다음 전 메뉴로 넓혀 가시면 됩니다.

원하시면 다음 단계로 **“공지사항 + 결제내역” API 스펙 초안**을 화면(screens.js) 기준으로 구체적으로 적어 드리겠습니다.
