# PG 통합관리자 - 로컬 개발 가이드

## 빠른 시작 (PostgreSQL 없이)

**1. 실행**
```bash
# 프로젝트 루트(PG)에서
cd pg-app
gradlew bootRun --args="--spring.profiles.active=dev"
```

또는 `로컬_실행.bat` 더블클릭 (Windows)

**2. 접속**
- http://localhost:8080/login.html
- 로그인: **admin** / **admin1!**

**3. 종료**
- 터미널에서 `Ctrl+C`

**4. 8080 포트 사용 중일 때**
```powershell
# Windows: 8080 사용 프로세스 확인 후 종료
netstat -ano | findstr :8080
taskkill /PID <PID> /F
```

---

## dev 프로파일 (H2 인메모리)

| 항목 | 설명 |
|------|------|
| DB | H2 인메모리 (PostgreSQL 불필요) |
| 데이터 | 앱 시작 시 자동 생성 (admin, 업체 3건 등) |
| ddl-auto | create-drop (재시작 시 스키마 초기화) |
| H2 콘솔 | http://localhost:8080/h2-console (JDBC URL: `jdbc:h2:mem:pgdev`) |

---

## IDE에서 실행

**Run Configuration 예시:**
- Main class: `com.pg.PgAppApplication`
- VM options: (없음)
- Program arguments: `--spring.profiles.active=dev`
- Working directory: `$MODULE_WORKING_DIR$` 또는 `pg-app`

---

## 프론트(site) + 백엔드 분리 실행

- **백엔드**: `pg-app` → http://localhost:8080
- **프론트**: Live Server 등으로 `site` → http://localhost:5500

이 경우 `site/js/config.js`에서 `PG_API_BASE = 'http://localhost:8080'` 설정 (또는 URL에 `?api=http://localhost:8080`)

---

## PostgreSQL로 로컬 개발 (선택)

DB가 이미 있다면:
```bash
set DB_PASSWORD=your_password
gradlew bootRun
# 또는 --spring.profiles.active=local + application-local.yml
```
