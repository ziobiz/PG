# PuTTY 접속 방법 (가상서버 SSH)

> Windows에서 PuTTY로 카페24 가상서버에 접속하는 순서입니다.

---

## 1. PuTTY 설치 (아직 없다면)

1. 브라우저에서 **https://www.putty.org** 접속
2. **Download PuTTY** 클릭
3. **Windows 64-bit** (또는 본인 PC에 맞는 버전) **putty.exe** 다운로드
4. 다운로드한 **putty.exe** 더블클릭해서 실행  
   (설치 없이 실행만 해도 됩니다. 자주 쓰려면 바탕화면에 바로가기 만들어 두세요.)

---

## 2. PuTTY에서 서버 접속

### 2-1. PuTTY 창이 열리면

- **Host Name (or IP address)** 칸에 **서버 IP 주소** 입력  
  예: `123.456.78.90` (카페24에서 받은 IP)
- **Port** 칸에 **22** 입력
- **Connection type** 에서 **SSH** 가 선택되어 있는지 확인
- **Open** 버튼 클릭

### 2-2. 처음 접속할 때 나오는 창 (Security Alert)

- "The server's host key is not cached..." 라는 창이 뜨면  
  → **Accept** 또는 **예** 클릭  
  (이 서버를 신뢰한다고 한 번 등록하는 것입니다.)

### 2-3. 로그인

- **login as:** 라고 나오면 **SSH 계정** 입력 후 Enter  
  예: `root` (카페24에서 안내한 계정명)
- **password:** 라고 나오면 **비밀번호** 입력 후 Enter  
  (입력해도 화면에는 **** 같이 안 보일 수 있습니다. 그대로 입력하고 Enter 치면 됩니다.)

### 2-4. 접속 성공

- `root@서버이름:~#` 또는 `root@서버이름:~$` 같은 글자가 보이면 **접속 성공**입니다.  
  이제 이 창에 명령어를 입력하는 것이 “서버 안에서 작업하는 것”입니다.

---

## 3. 요약 (한 번에 보기)

| 단계 | 할 일 |
|------|--------|
| 1 | PuTTY 실행 |
| 2 | Host Name에 **서버 IP** 입력 |
| 3 | Port **22**, Connection type **SSH** 확인 |
| 4 | **Open** 클릭 |
| 5 | Security Alert 나오면 **Accept** |
| 6 | **login as:** 에 계정(예: root) 입력 → Enter |
| 7 | **password:** 에 비밀번호 입력 → Enter |
| 8 | `root@...:~#` 가 보이면 접속 완료 |

---

## 4. 접속이 안 될 때

- **Connection timed out**  
  → 서버 IP가 맞는지, 카페24에서 서버가 정상 가동 중인지 확인. 방화벽에서 포트 22가 막혀 있지 않은지 확인.
- **Access denied** / **비밀번호 거부**  
  → 계정명·비밀번호가 카페24에서 안내한 것과 같은지 확인. 비밀번호는 대소문자 구분됨.
- **Network error**  
  → 인터넷 연결 확인. 같은 네트워크에서 다른 PC로 테스트해 보기.

문제가 생기면 **에러 메시지 전체**를 복사해서 Cursor에 붙여 넣고 “이 에러가 났어”라고 하면, 원인과 해결 방법을 안내해 드릴 수 있습니다.
