# 가상서버 설치 완료 후 할 일 (체크리스트)

> Ubuntu 22.04 가상서버 일반형 설치가 끝난 뒤 순서대로 진행하세요.

---

## 1단계: 접속 정보 확인·저장

- [ ] 카페24 호스팅 관리 페이지에서 **서버 접속 정보** 확인
- [ ] 아래 세 가지를 **메모장 등에 저장** (잃어버리지 마세요)
  - **서버 IP 주소** (예: 123.456.78.90)
  - **SSH 계정** (예: root 또는 안내된 계정명)
  - **비밀번호** (또는 키 파일 안내)

---

## 2단계: SSH 접속 준비 (Windows 기준)

- [ ] **PuTTY** 설치 (아직 없다면)
  - https://www.putty.org 에서 다운로드
  - 또는 Windows 10/11에서는 **PowerShell** 또는 **명령 프롬프트**에서 `ssh` 명령 사용 가능
- [ ] PuTTY 실행 후:
  - **Host Name**: 서버 IP 주소 입력
  - **Port**: 22
  - **Connection type**: SSH
  - **Open** 클릭 → 계정·비밀번호 입력해서 접속

**Mac** 이라면: **터미널** 앱 열고 `ssh 계정@서버IP` 입력 (예: `ssh root@123.456.78.90`)

- [ ] 접속 성공 시 `root@서버이름:~#` 같은 글자가 보이면 성공 (이제 “서버 안”에 있는 상태)

---

## 3단계: 서버 안에 Java 17, PostgreSQL 설치

**아래 명령어를 SSH 접속한 창에 순서대로 붙여 넣고 Enter** (한 번에 여러 줄 붙여 넣어도 됨).

### 3-1. 패키지 업데이트

```bash
sudo apt update && sudo apt upgrade -y
```

(끝날 때까지 기다린 뒤 다음으로)

### 3-2. Java 17 설치

```bash
sudo apt install -y openjdk-17-jdk
```

설치 확인:

```bash
java -version
```

`openjdk version "17` 로 시작하는 줄이 나오면 성공.

### 3-3. PostgreSQL 설치

```bash
sudo apt install -y postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### 3-4. 개발용 DB·계정 만들기

`여기에_비밀번호` 부분만 **본인이 정한 비밀번호**로 바꿔서 실행하세요.

```bash
sudo -u postgres psql -c "CREATE USER pgadmin WITH PASSWORD '여기에_비밀번호';"
sudo -u postgres psql -c "CREATE DATABASE pgdev OWNER pgadmin;"
```

---

## 4단계: 여기까지 완료하면

- [ ] Java 17 설치됨
- [ ] PostgreSQL 설치·실행됨
- [ ] 개발용 DB `pgdev` 와 계정 `pgadmin` 생성됨

**다음**: Cursor로 Spring Boot 프로젝트를 만들고, JAR 파일을 이 서버에 올린 뒤 `java -jar 프로젝트명.jar` 로 실행하면 됩니다. (자세한 건 `카페24_개발서버_구축가이드.md` 6단계 참고)

---

## 막히면

- **에러 메시지**가 나오면: 그 문구 전체를 복사해서 Cursor에 붙여 넣고 “이 에러 어떻게 해결해?”라고 물어보세요.
- **어느 단계에서 막혔는지** (예: “2단계 SSH 접속이 안 돼”)를 알려주시면, 그 단계만 구체적으로 안내해 드릴 수 있습니다.
