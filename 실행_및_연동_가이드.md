# PG 통합관리자 – 실행 및 연동 가이드

## 1. 백엔드 (pg-app) 실행

- **필요 조건**: Java 17, PostgreSQL (또는 application-local.yml에 설정한 DB)
- **실행**:
  ```bash
  cd pg-app
  ./gradlew bootRun
  ```
  또는 IDE에서 `PgAppApplication` 실행.
- **기본 포트**: 8080
- **초기 데이터**: 최초 실행 시 admin 사용자(admin / admin1!), 공지 3건, 업체 3건, 결제거래 15건이 자동 생성됩니다.

## 2. 프론트(site)와 연동

- **site**는 정적 파일이므로 웹 서버로 제공하거나 `index.html`을 브라우저로 엽니다.
- **API 주소 설정**: `site/js/config.js`에서 `PG_API_BASE`를 백엔드 주소로 설정합니다.
  - 로컬에서 pg-app을 8080으로 띄운 경우: `http://localhost:8080`
  - 같은 도메인에서 서빙하면 빈 문자열 `''`로 두고 프록시로 `/api`를 pg-app으로 보냅니다.

## 3. 로그인 및 화면 조회

1. 브라우저에서 `site/login.html` (또는 `/site/login.html`) 접속
2. **아이디**: admin, **비밀번호**: admin1! 입력 후 로그인
3. 로그인 성공 시 `index.html`로 이동하며, 메뉴에서 **공지사항**, **결제내역**, **업체관리** 등을 선택 후 **검색** 버튼을 누르면 실제 API로 조회됩니다.

## 4. 구현된 API 요약

| 기능       | 메서드 | 경로                  | 비고                    |
|------------|--------|------------------------|-------------------------|
| 로그인     | POST   | /api/auth/login        | JSON body: username, password |
| 공지 목록  | GET    | /api/system/notice     | searchTitle, searchFromDate, searchToDate, page, size |
| 결제 내역  | GET    | /api/calc/payList      | searchCompNm, searchFromDate, searchToDate, page, size |
| 업체 목록  | GET    | /api/comp/list         | searchCompId, searchCompNm, page, size |
| 업체 등록  | POST   | /api/comp/register     | compNm, compDiv (form params) |

모든 API(로그인 제외)는 `Authorization: Bearer <token>` 헤더로 인증합니다. 로그인 응답의 `token`을 sessionStorage에 저장해 사용합니다.

## 5. CORS

- pg-app은 `application.yml`의 `app.cors.allowed-origins`에 설정된 오리진만 허용합니다.
- 로컬에서 site를 다른 포트(예: 5500)로 띄우면 `http://localhost:5500`을 허용 목록에 넣어 두었으므로 그대로 사용 가능합니다.

## 6. ChillPay (칠리페이) API 연동

- **결제 페이지**: `http://localhost:8080/pay.html` (또는 배포 URL/pay.html)
- **설정**: **본사설정 > API 구성 세팅** 화면의 **ChillPay (칠리페이) 연동** 섹션에서 입력
  - Merchant Code, API Key, MD5 Secret Key, Route No, Sandbox 사용 여부
- **API 엔드포인트**:
  - `GET /api/pay/chillpay/config`: 결제 페이지용 설정 (CCD 스크립트 URL, `directCreditApiUrl`, `redirectPaymentPageUrl`, `paymentAppsrvV2Url`, Merchant Code 등)
  - `POST /api/pay/chillpay/request`: DirectCredit 결제 요청 (directCreditToken, amount, orderNo 등)
- **DirectCreditToken**: ChillPay CCD 스크립트(`ccdpayment.js`)에서 카드 입력 후 발급되는 토큰. 샌드박스 데모에서 발급 후 입력란에 붙여넣어 테스트 가능.
- **호스티드(리다이렉트) 결제·v2 API URL**: `docs/ChillPay_연동_URL.md` 참고. `pay.html` 상단 안내 블록에서도 동일 값 확인 가능.
