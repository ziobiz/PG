# 개발 환경 및 권장 스택

> 방향: 기존 소스는 설계/명세만 참고, 구현은 새 스택으로 처음부터 (하이브리드)

---

## 1. PG·결제 쪽에서 Java(JSP/Spring)가 많이 쓰이는 이유

결제·PG·금융 도메인에서 **Java 기반(JSP, Spring)** 이 많이 쓰이는 이유는 대략 아래와 같습니다.

| 이유 | 설명 |
|------|------|
| **보안** | 타입 안정성, 메모리 관리, 금융·결제 관련 보안 가이드·인증 사례가 Java 기준으로 많음. |
| **트랜잭션·안정성** | DB 트랜잭션, 동시성 제어, 장시간 운영 경험이 쌓여 있고, Spring의 트랜잭션 관리가 잘 정리되어 있음. |
| **PG·은행 연동** | 국내 PG사·은행·VAN SDK/API가 **Java 샘플·문서**를 먼저 제공하는 경우가 많음. |
| **규제·감사** | 금융권·결제 쪽 감사·컴플라이언스에서 Java/엔터프라이즈 스택을 선호하는 경향이 있음. |
| **유지보수·인력** | 국내에서 결제·백오피스 개발 인력이 Java/Spring 경험자 비중이 큼. |

그래서 **“JSP vs PHP”** 로 보면, 이 도메인에서는 **JSP(Java) 쪽이 보안·연동·운영 측면에서 더 잘 맞고**, 보완(보안·트랜잭션·SDK 지원)이 명확하다고 보는 게 맞습니다.  
다만 **“JSP 그대로”** 가 아니라 **“Java + 현대 웹 스택”** 으로 가는 것을 권장합니다.

---

## 2. JSP를 “그대로” 쓰지 말고, “Java 기반 + 현대 뷰”로 가는 이유

- **JSP 자체**는 20년 넘은 기술이고, 스크립트 안에 로직이 섞이기 쉬워 유지보수·테스트가 어렵습니다.
- **Spring Boot + 서버 렌더링** 이라면 **Thymeleaf** 같은 템플릿을 쓰면 “JSP처럼 화면을 서버에서 찍는” 구조는 유지하면서, 문법·구조가 더 단순하고 다국어·레이아웃 관리가 쉽습니다.
- **API + 프론트 분리**로 가면 **React/Vue** 등으로 UI를 만들 수 있어, 다국어·복잡한 화면·재사용에 유리합니다.

즉, **언어/플랫폼은 Java로 가되, 뷰는 JSP 대신 “Thymeleaf 또는 React/Vue”** 를 쓰는 구성을 권장합니다.

---

## 3. 권장 개발 환경 (스택)

### 백엔드 (서버·DB·결제 로직)

| 구분 | 권장 | 비고 |
|------|------|------|
| **언어·프레임워크** | **Java 17+ / Spring Boot 3.x** | PG·금융 도메인·기존 명세(Java)와 맞추기 좋음 |
| **DB** | **PostgreSQL** (또는 MySQL) | 기존 Oracle 스키마를 “명세”로 참고해 재설계 |
| **ORM** | **JPA (Hibernate)** | 트랜잭션·정산 데이터 관리에 적합 |
| **API** | **REST** (JSON) | 프론트·다른 시스템 연동용 |
| **인증·보안** | **Spring Security** | 로그인, OTP, 비밀번호 정책, 세션 등 |
| **빌드** | **Gradle** (또는 Maven) | Java 표준 도구 |

### 뷰 (화면) — 두 가지 중 선택

| 방식 | 기술 | 적합한 경우 |
|------|------|-------------|
| **A. 서버 렌더링** | **Thymeleaf** | JSP처럼 “서버에서 HTML 찍기”를 유지하고 싶을 때, 백오피스 위주일 때 |
| **B. SPA (API 연동)** | **React** 또는 **Vue** | 화면이 복잡·다국어·재사용을 중시할 때, 이미 만든 fxhj-login-ui를 React/Vue로 옮길 때 |

- **당장은 A(Thymeleaf)** 로 시작해도 되고,  
- 나중에 “관리자 화면을 더 풍부하게” 하려면 **B(React/Vue)** 로 점진적으로 바꿔도 됩니다.

### 기타

| 구분 | 권장 |
|------|------|
| **다국어** | Spring MessageSource + properties (또는 DB) |
| **PG 연동** | Spring Boot 안에 “PG 어댑터” 모듈로 구성 (이니시스, 해외 PG 등 구현체 분리) |
| **실행·배포** | JAR 실행, Docker 가능 |

---

## 4. 정리

- **개발 환경은 “Java 기반”** 으로 가는 것이 좋습니다. (보안·PG 연동·인력·기존 명세와의 일관성)
- **JSP는 “명세/레거시 참고”만 하고**, 새 구현에서는 **Spring Boot + Thymeleaf(또는 React/Vue)** 를 쓰는 구성을 권장합니다.
- **백엔드**: Java 17+, Spring Boot 3.x, PostgreSQL, JPA, Spring Security, REST.  
- **뷰**: Thymeleaf(서버 렌더링) 또는 React/Vue(SPA) 중 선택.

이렇게 정해 두면, “기존 소스는 설계/명세로만 쓰고, 구현은 새 스택(Java + Spring Boot + 현대 뷰)으로 처음부터” 하는 방향과 잘 맞습니다.  
다음 단계로 **프로젝트 폴더 구조**와 **1단계 작업(로그인·메인)** 부터 구체화할 수 있습니다.
