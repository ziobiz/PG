# 접근 구조 & 사용자 구분 & 로컬 테스트

## 1. 백엔드 접근 vs 프론트 접근 (같을 수도, 다를 수도 있음)

- **한 서버로 운영할 때**  
  - 백엔드(pg-app)가 **API**(`/api/**`)와 **프론트 화면**(`site` 폴더)을 **같은 주소**로 서빙합니다.  
  - 예: `http://localhost:8080` 에서  
    - `http://localhost:8080/login.html` → 로그인 페이지  
    - `http://localhost:8080/index.html` → 메인(관리자 화면)  
    - `http://localhost:8080/api/...` → API  
  - 이때 **백엔드 접근 주소 = 프론트 접근 주소** (같은 사이트).

- **분리해서 운영할 때**  
  - 백엔드: `http://api.example.com` (또는 `http://localhost:8080`)  
  - 프론트: `http://admin.example.com` (또는 별도 포트)  
  - 이때 **백엔드 접근 주소 ≠ 프론트 접근 주소**이고, `site/js/config.js`에서 `PG_API_BASE`만 백엔드 주소로 맞추면 됩니다.

정리하면, **반드시 다른 사이트일 필요는 없고**, 한 서버로 같이 띄우면 **같은 주소**로 접근할 수 있습니다.

---

## 2. 사용자 vs 슈퍼 관리자 구분

- DB·백엔드에는 이미 **역할(role)** 이 있습니다.  
  - `tb_user.role`: 예) `USER`, `ADMIN`  
  - 초기 계정: `admin` / `admin1!` → role `ADMIN`

- **나누는 방식 예시**  
  - **일반 사용자(USER)**: 공지 조회, 본인 결제/정산 조회 등만 허용  
  - **슈퍼 관리자(ADMIN)**: 업체 등록·수정, 수수료 설정, 전체 조회 등 허용  

- **적용 방법**  
  - 로그인 API 응답에 `role` 포함  
  - 프론트에서 `role`에 따라 메뉴/버튼 표시·숨김  
  - 백엔드 API에서 `@PreAuthorize("hasRole('ADMIN')")` 등으로 슈퍼 관리자 전용 API 제한  

지금은 **한 관리자 화면**만 있고, 위처럼 **역할만 나누면** 사용자/슈퍼 관리자 구분이 됩니다.  
(별도 “사용자 전용 사이트”를 만들 필요는 없고, 같은 사이트에서 메뉴/기능만 역할별로 제한하면 됩니다.)

---

## 3. 로컬에서 테스트할 수 없으면 서버에 올려야 하나?

**아니요. 로컬에서만 테스트해도 됩니다.** 서버에 올리지 않아도 됩니다.

- **로컬 한 서버로 테스트 (권장)**  
  1. 프로젝트 루트가 `PG` 라고 할 때, **`PG/pg-app`** 에서 백엔드를 실행합니다.  
     - 예: `cd pg-app` 후 `./gradlew bootRun` (또는 IDE에서 `PgAppApplication` 실행)  
  2. pg-app 설정에 따라 **`PG/site`** 폴더를 **같은 서버(8080)** 에서 함께 서빙되도록 해 두었습니다.  
  3. 브라우저에서 **`http://localhost:8080/login.html`** 로 접속합니다.  
  4. 로그인(admin / admin1!) 후 `index.html` 로 이동해 공지·결제·업체 메뉴를 사용하면 **같은 주소(로컬)에서** 백엔드·프론트 모두 동작합니다.

- **로컬에서 “프론트만 별도 포트”로 띄우는 경우**  
  - 예: Live Server로 `site` 만 `http://localhost:5500` 에 띄우고, pg-app은 `http://localhost:8080` 에 실행  
  - 이때는 `site/js/config.js` 에서 **`PG_API_BASE = 'http://localhost:8080'`** 로 두면, **백엔드 접근 주소**만 8080으로 가고 프론트 접근 주소는 5500입니다.

- **서버에 올리는 경우**  
  - 로컬 테스트가 불가능한 환경이거나, 실제 운영/배포를 할 때만 서버에 배포하면 됩니다.  
  - 그때는 **한 서버에 pg-app + site 같이 두고** 같은 도메인으로 서빙하거나,  
  - 백엔드와 프론트를 **다른 도메인**으로 두고 `PG_API_BASE` 만 서버 백엔드 주소로 설정하면 됩니다.

---

## 4. 요약

| 항목 | 설명 |
|------|------|
| 백엔드 vs 프론트 접근 | **한 서버**로 띄우면 **같은 주소**, 분리하면 **다른 주소**. 선택 사항입니다. |
| 사용자 vs 슈퍼 관리자 | **같은 사이트**에서 **역할(USER/ADMIN)** 로 구분하면 됩니다. |
| 로컬 테스트 | **로컬만으로 가능**합니다. pg-app 실행 후 `http://localhost:8080/login.html` 로 접속해 테스트하면 됩니다. |
| 서버 배포 | 로컬 테스트가 되므로 **꼭 서버에 올리지 않아도** 되고, 운영/배포가 필요할 때만 서버에 올리면 됩니다. |
