# 카페24 개발 서버 구축 가이드 (왕초보용)

> 목표: 권장 개발 환경(Java 17, Spring Boot, PostgreSQL)을 올릴 **간단한 개발 서버**를 카페24에서 임대해 구축하기  
> 대상: 개발/코딩 경험이 전혀 없는 분도 Cursor와 이 가이드를 따라 할 수 있도록 단계별 안내

---

## 1. 어떤 서버를 골라야 하나요?

### 1.1 카페24에서 쓸 수 있는 서버 종류

| 서비스 | 적합한 용도 | 우리 프로젝트 |
|--------|-------------|----------------|
| **Tomcat JSP 호스팅** | 예전 방식 JSP, WAR 배포, MySQL만 사용 | ❌ Spring Boot 3 + PostgreSQL 불가 |
| **가상서버(VPS)** / **카페24 클라우드** | 서버 전체를 직접 설정 (Java 17, PostgreSQL 등 설치) | ✅ **이걸 선택** |

**정리**: Spring Boot 3 + Java 17 + PostgreSQL을 쓰려면 **가상서버(VPS)** 또는 **카페24 클라우드**를 임대해야 합니다.

### 1.2 개발용으로 추천하는 제품·스펙

- **제품**: **가상서버 호스팅 — 일반형** (선택 확정)
- **개발 서버용**: 제공되는 일반형 스펙 중 **가장 낮은 단계**로 시작해도 됨 (예: CPU 2, RAM 2GB 수준). 필요 시 나중에 업그레이드.
- **OS**: **Ubuntu 22.04 LTS** 선택 (가능한 경우). 없으면 Ubuntu 20.04 LTS.
- **비용**: 가상서버 일반형 요금제에 따름.

> 견적·상세: 카페24 호스팅 → 서버호스팅 → 가상서버호스팅 → 일반형에서 확인.

---

## 2. 서버 구축 순서 (큰 흐름)

1. **카페24에서 가상서버(클라우드) 신청** → OS Ubuntu 22.04 선택  
2. **서버 접속** (SSH) 가능하게 하기  
3. **서버 안에 설치**: Java 17, PostgreSQL, (선택) Nginx  
4. **Spring Boot 프로젝트** 올려서 실행  
5. **방화벽** 설정 (필요 시)

아래는 2~4를 “왕초보도 따라 할 수 있게” 단계별로 적은 것입니다.  
**Cursor에게 “이 단계에서 막혔다”고 말하면서, 해당 단계 번호와 화면/에러 메시지를 알려주면, Cursor가 다음 명령어나 수정 방법을 구체적으로 알려줄 수 있습니다.**

---

## 3. 단계 1: 카페24 가상서버 신청 (일반형)

1. **카페24 호스팅** 접속: https://hosting.cafe24.com  
2. **서버호스팅** → **가상서버호스팅** → **일반형** 선택 후 **신청**  
3. **신청 시 선택**:
   - **제품**: 가상서버 **일반형** (가장 낮은 스펙으로 시작해도 됨)
   - **OS**: **Ubuntu 22.04 LTS** (가능하면). 없으면 Ubuntu 20.04 LTS
   - **공인 IP**: 1개 (필수인 경우 체크)
4. 결제·생성 완료 후, **접속 정보**를 반드시 저장:
   - **IP 주소** (예: 123.456.78.90)
   - **SSH 접속 계정** (예: root 또는 안내된 계정명)
   - **비밀번호** (또는 키 파일)

이 접속 정보는 “서버 문 열쇠”이므로 잃어버리지 말고 안전한 곳에 보관하세요.

---

## 4. 단계 2: 서버 접속 (SSH) — 처음 한 번만

- **Windows**: **PuTTY** 또는 **PowerShell** 사용  
  - PuTTY: https://www.putty.org 에서 다운로드 후, 호스트에 “서버 IP” 입력, 포트 22, 연결 후 계정/비밀번호 입력  
- **Mac**: “터미널” 앱 실행 후 `ssh 계정@서버IP` 입력 (예: `ssh root@123.456.78.90`)

접속되면 `root@서버이름:~#` 같은 글자만 나오는 화면이 나옵니다. 이게 “서버 안”입니다.

---

## 5. 단계 3: 서버 안에 필요한 프로그램 설치

아래 명령어는 **서버에 SSH로 접속한 뒤**, **순서대로** 입력합니다.  
(복사해서 붙여 넣은 뒤 Enter 치면 됩니다. 한 번에 여러 줄 붙여 넣어도 됩니다.)

### 5.1 최신 패키지 목록 반영

```bash
sudo apt update && sudo apt upgrade -y
```

### 5.2 Java 17 설치

```bash
sudo apt install -y openjdk-17-jdk
```

설치 확인:

```bash
java -version
```

`openjdk version "17.x.x"` 비슷하게 나오면 성공입니다.

### 5.3 PostgreSQL 설치

```bash
sudo apt install -y postgresql postgresql-contrib
```

PostgreSQL 서비스 켜기 및 부팅 시 자동 시작:

```bash
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

개발용 DB·계정 만들기 (비밀번호는 본인이 정한 걸로 바꾸세요):

```bash
sudo -u postgres psql -c "CREATE USER pgadmin WITH PASSWORD '여기에_비밀번호';"
sudo -u postgres psql -c "CREATE DATABASE pgdev OWNER pgadmin;"
```

### 5.4 (선택) Nginx — 나중에 웹 앞단·HTTPS 쓰고 싶을 때

```bash
sudo apt install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

---

## 6. 단계 4: Spring Boot 프로젝트 올리고 실행

1. **로컬 PC**에서 Cursor로 Spring Boot 프로젝트를 **JAR**로 빌드합니다.  
   (Cursor에게 “이 프로젝트 Gradle로 JAR 빌드하는 방법 알려줘”라고 하면 명령어를 알려줍니다.)
2. 만든 **JAR 파일**을 서버로 복사합니다.  
   - 예: FileZilla(SFTP) 또는 `scp` 명령  
   - 예: `scp build/libs/프로젝트명.jar root@서버IP:/home/`
3. **서버에 SSH 접속**한 뒤, JAR 있는 폴더로 이동해서 실행:

```bash
cd /home
java -jar 프로젝트명.jar
```

- 포트를 바꾸고 싶으면: `java -jar 프로젝트명.jar --server.port=8080`
- **백그라운드**로 돌리려면: `nohup java -jar 프로젝트명.jar &`

브라우저에서 `http://서버IP:8080` (실제 포트에 맞게) 로 접속해 보면 됩니다.

---

## 7. 방화벽 (필요할 때)

카페24 클라우드는 **방화벽** 기능을 제공합니다.  
- 개발 서버에서는 **22(SSH)**, **8080**(Spring Boot), **5432**(PostgreSQL, 서버 안에서만 쓸 거면 안 열어도 됨) 정도만 열어 두면 됩니다.  
- 카페24 콘솔에서 “방화벽 규칙” 또는 “보안 그룹” 메뉴를 찾아, 위 포트만 허용하도록 설정하면 됩니다.

---

## 8. 한 번에 정리: “가장 합리적이고 보완이 완벽한” 개발 서버 구성

| 항목 | 선택 |
|------|------|
| **서버** | 카페24 가상서버 호스팅 **일반형** |
| **스펙** | m3.small 또는 m3.medium (개발용) |
| **OS** | Ubuntu 22.04 LTS |
| **설치** | Java 17(OpenJDK), PostgreSQL, (선택) Nginx |
| **앱** | Spring Boot 3 JAR 실행 |
| **DB** | PostgreSQL (개발 DB 하나 생성) |
| **접속** | SSH로 서버 접속 → 명령어로 설치·실행 |

이 구성을 따르면, 권장 개발 환경(Java 17, Spring Boot, PostgreSQL)과 **가장 잘 맞고**, 보완(보안·트랜잭션·나중에 확장)도 잘 맞는 **간단한 개발 서버**가 됩니다.

---

## 9. Cursor로 “완벽하게” 개발할 수 있을까? (왕초보 분께)

### 9.1 할 수 있는 것

- **코드 작성**: Cursor가 Spring Boot, 로그인, DB 연동, API, 화면(Thymeleaf/React) 코드를 생성·수정해 줄 수 있습니다.  
- **명령어·설정**: “Gradle로 빌드해줘”, “이 에러 어떻게 고쳐?”처럼 말하면, 실행할 명령어나 수정할 부분을 단계별로 알려줄 수 있습니다.  
- **서버 구축**: 이 문서처럼 “이 단계까지 했는데 다음이 뭐예요?”라고 하면, 그다음 명령어나 설정을 이어서 안내받을 수 있습니다.  
- **학습**: 코드나 명령어가 왜 그런지, 무엇을 하는지 설명해 달라고 하면 이해하기 쉽게 설명해 줍니다.

즉, **소스/코딩을 전혀 모르는 왕초보라도, Cursor의 도움으로 개발을 “많이” 진행할 수 있습니다.**

### 9.2 “완벽하게”에 대해

- **완벽하게** = “설계한 기능이 안정적으로 동작하고, 보안·결제 같은 부분이 실서비스 수준이다”라고 하면,  
  - **처음부터 끝까지 Cursor만으로** 가려면, **단계를 잘 나누고, 한 단계씩 “동작 확인 → 다음 단계”** 로 가는 방식이 좋습니다.  
  - **결제·PG·금융** 같은 부분은 나중에 **한 번쯤 전문 개발자/감사**가 검토해 주는 편이 더 안전합니다.
- **실질적으로**:  
  - “기획·설계 + Cursor로 구현 + 본인이 테스트·수정 요청”까지는 **Cursor 도움으로 충분히 가능**하고,  
  - “완벽한 상용 서비스”는 **Cursor로 대부분 만든 뒤, 중요한 부분만 전문가가 점검**하는 방식이 가장 합리적입니다.

### 9.3 왕초보가 하기 좋은 습관

1. **한 번에 하나씩**: 서버 구축이든, 로그인이든, “지금 이 단계만” 정해서 Cursor에게 요청하기.  
2. **에러 나오면 그대로 전달**: 에러 메시지 전체를 복사해서 Cursor에 붙여 넣고 “이걸 어떻게 고쳐?”라고 하기.  
3. **명령어는 그대로 실행**: Cursor가 알려준 명령어를 터미널/SSH에 그대로 붙여 넣고 실행해 보기.  
4. **이 가이드 + Cursor**: 이 문서의 “단계 1~4”를 따라 하다가 막히는 곳에서 Cursor에게 “카페24_개발서버_구축가이드 3단계에서 막혔다”처럼 말하면, 그 단계에 맞는 구체적인 명령어나 설정을 받을 수 있습니다.

---

## 10. 다음 단계

1. **카페24에서 가상서버(클라우드) 신청** (Ubuntu 22.04, m3.small 이상)  
2. **접속 정보** 저장  
3. **이 문서 4~6단계** 따라 서버 접속 → Java 17, PostgreSQL 설치 → Spring Boot JAR 실행  
4. **Cursor로** Spring Boot 프로젝트 생성·수정하면서, “로컬에서 실행 → 서버에 JAR 올려서 실행” 순서로 개발 진행  

중간에 막히는 단계가 생기면, **“몇 단계에서, 어떤 화면/에러가 나온다”**고 Cursor에게 말해 주면, 그 다음 단계를 구체적으로 안내해 줄 수 있습니다.
