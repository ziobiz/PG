# 개발 방향 분석: 기존 소스 기반 vs 신규 개발

> 목표: 다중 해외 PG 연동, 다국어, 유연한 과금/정산 + fxhj 수준 UI/보안(OTP 등)

---

## 1. 기존 소스 요약

| 항목 | 내용 |
|------|------|
| 개발 시기 | 2011년경 (~15년 경과) |
| 웹 | Spring MVC 2.x, JSP, JSTL, Resin |
| DB | Oracle 10g |
| 결제 | 이니시스(INIpay) 단일 연동, TCP 소켓 결제 서버 |
| 인증 | ID/PW + 세션 (OTP·비밀번호 변경 없음) |
| 강점 | 가맹점/총판/거래/정산 도메인·DB 설계가 이미 반영됨 |

---

## 2. 옵션 A: 기존 소스 기반 확장

### 장점
- **도메인·DB 재사용**: PG_MERCHANT, TB_GROUP_MNG, PG_TRNSCTN, PG_MERCHANT_BILL, TB_SETTLE 등 이미 설계되어 있어, “무엇을 관리할지”를 다시 짜지 않아도 됨.
- **운영 경험 반영**: 실제 운영된 구조라 예외·엣지 케이스가 일부 코드/DB에 녹아 있을 수 있음.
- **초기 구간 단축**: 로그인·거래 조회·정산 흐름이 있으므로, “추가/교체” 위주로 작업 가능.

### 단점
- **기술 부채**: Spring 2.x, JSP, Resin, Oracle 10g는 구버전. 보안 패치·인력·호환성 부담이 큼.
- **구조적 한계**: PG 연동이 Credit/WebCredit 안에 이니시스만 하드코딩되어 있어, “PG 하나 더 붙이기”가 단순 추가가 아니라 전면 리팩터링에 가깝게 됨.
- **다국어**: JSP/메시지가 한글 위주·하드코딩. 리소스 번들로 바꾸려면 전반 수정 필요.
- **OTP·보안**: 참고 사이트(fxhj) 수준으로 올리려면 로그인·세션·비밀번호 정책을 새로 설계·구현해야 함.
- **유지보수 비용**: 레거시 스택 유지 시 장기적으로 인력·도구 지원이 어렵고, 신규 PG·규제 대응 시 수정 범위가 커짐.

### 결론 (A)
- “기존 소스를 **그대로** 두고 확장”하는 방식은 **비추천**.
- 목표(다중 PG, 다국어, 유연한 정산, OTP)를 만족하려면 결국 **대규모 개편**이 필요하고, 그렇게 하면 레거시 위에 레거시가 쌓여 비용만 커짐.

---

## 3. 옵션 B: 신규 개발 (처음부터 재구현)

### 장점
- **스택 선택 자유**: Spring Boot + JPA, PostgreSQL/MySQL, REST API, React/Vue 등 현대 스택으로 설계 가능.
- **아키텍처 정리**: 처음부터 PG 어댑터, 다국어, 정산 정책 엔진을 넣어 설계할 수 있음.
- **보안·OTP**: 로그인·2단계 인증·비밀번호 정책을 표준(Spring Security 등)으로 구현하기 쉬움.
- **장기 유지보수**: 인력·도구·클라우드 연동이 수월함.

### 단점
- **초기 기간·비용**: 분석·설계·개발·테스트까지 기간이 길어짐.
- **도메인 누락 위험**: 기존 시스템에만 있던 “암묵적” 규칙·예외를 놓치기 쉽음.

### 결론 (B)
- **목표와 장기 운영**을 생각하면 **신규 개발이 유리**.
- 단, “완전 백지”가 아니라 **기존 소스·DB·화면은 ‘명세서’로 활용**하는 것이 좋음.

---

## 4. 옵션 C: 하이브리드 (권장)

**“신규 스택으로 다시 만들되, 기존 소스·DB·화면을 명세·레퍼런스로 100% 활용”**

### 원칙
1. **기술은 신규**: Spring Boot, 현대 DB(PostgreSQL 등), REST API, 프론트(React/Vue 또는 서버 렌더링), OTP·보안 표준 적용.
2. **도메인·기능은 기존 따름**:  
   - 기존 테이블 구조(PG_MERCHANT, TB_GROUP_MNG, PG_TRNSCTN, PG_MERCHANT_BILL, TB_SETTLE, TB_VAN 등)를 **참고**해 새 스키마 설계(이름·관계·컬럼 의미 유지, 필요 시 확장).  
   - 가맹점/총판/거래/정산/한도 등 **비즈니스 로직**은 기존 소스·메뉴얼을 읽고 “동작 규칙”을 문서로 뽑은 뒤, 새 코드로 구현.
3. **UI/UX는 참고 사이트(fxhj) 기준**: 로그인(OTP 포함), 메인, 정산, 과금 등 화면은 fxhj·otl3를 참고해 **새 프론트**로 구현(이미 만든 fxhj-login-ui도 이 흐름에 활용).

### 장점
- 기존 소스의 **도메인·운영 노하우**는 살리면서, **기술 부채**는 만들지 않음.
- 다중 PG, 다국어, 유연한 정산을 **처음 설계**에 넣을 수 있음.
- 단계적으로 이전 가능: 예) 1단계 로그인·메인, 2단계 거래/정산 조회, 3단계 결제·PG 연동.

### 단점
- “기존 코드 복붙”보다는 설계·구현 공수가 듦. 대신 장기적으로 유지보수·확장이 훨씬 수월함.

---

## 5. 요약 및 권장

| 구분 | 기존 소스 확장 (A) | 신규 개발 (B) | 하이브리드 (C) |
|------|---------------------|---------------|-----------------|
| 기술 스택 | 레거시 유지 | 전부 신규 | **신규** |
| 도메인/DB | 그대로 사용 | 처음 설계 | **기존 참고해 재설계** |
| UI/기능 | 기존 + 부분 수정 | 처음 설계 | **fxhj·기존 참고** |
| 다중 PG·다국어 | 구조상 어렵고 비용 큼 | 설계부터 반영 가능 | **설계부터 반영** |
| OTP·보안 | 별도 대규모 수정 | 표준으로 구현 용이 | **표준으로 구현** |
| 장기 유지보수 | 불리 | 유리 | **유리** |

**권장: 옵션 C (하이브리드)**  
- **새 스택으로 새 시스템을 만들고**,  
- **기존 소스·DB·화면은 “명세·레퍼런스”로만 사용**해서,  
- 도메인은 그대로 살리되 기술 부채는 줄이는 방식.

---

## 6. 다음 단계 제안 (하이브리드 선택 시)

1. **도메인·DB 정리**  
   - 기존 Oracle 테이블·컬럼·관계를 문서로 정리하고,  
   - “새 DB(예: PostgreSQL) 스키마 초안”으로 옮기기(이름·의미 유지, 필요 시 확장).

2. **기능 목록 작성**  
   - 기존 소스·메뉴얼·fxhj 화면을 보고 “로그인/메인/가맹점/총판/거래/정산/과금/코드/VAN…” 등 **기능 목록** 정리.

3. **신규 프로젝트 골격**  
   - Spring Boot + DB + REST(또는 서버 렌더링) + 프론트(fxhj-login-ui 확장 등) 골격을 만들고,  
   - 1단계: 로그인(OTP)·메인·권한만 구현한 뒤,  
   - 2단계부터 거래/정산/다중 PG/다국어 순으로 확장.

원하시면 “하이브리드로 진행한다”고 가정하고,  
- **신규 프로젝트 폴더 구조**,  
- **1단계 작업 목록(로그인·메인)**  
까지 구체적으로 잡아 드리겠습니다.
