# 오늘 개발 작업 요약 (2026-02-23)

## 저장 상태
- **모든 수정 파일은 디스크에 저장된 상태입니다.**
- 이 프로젝트는 Git 저장소가 아니므로, 버전 관리를 원하시면 아래처럼 초기화 후 커밋하시면 됩니다.
  ```bash
  cd d:\Delopment\PG
  git init
  git add .
  git commit -m "오늘 개발 저장: 연동·본사정보·리스크관리 메뉴"
  ```

---

## 오늘 반영된 작업

### 1. 연동·입력창 점검 및 보완
- **업체 등록**: `parentComp` → `parentId` 연동 (백엔드에서 parentComp 숫자 파싱)
- **로그인**: 헤더 표시용 `loginId` 저장, 로그인된 상태면 로그인 페이지에서 index로 리다이렉트
- **API 구성 세팅**: DB 저장/조회 연동 (`tb_hq_api_config`, `HqApiConfig` 엔티티)

### 2. 업체정보 = 본사 정보
- **의미 정리**: 업체정보 = 상위 본사(우리)가 본사 권한을 준 그 회사의 정보
- **입력창**: fxhj 스타일 반영 — 본사 정보(업체정보), 업태/종목/팩스 추가, 상위 본사 라벨·placeholder 정리
- **업체정보조회**: 상세 폼(본사 정보 상세) + 조회/수정 저장 연동
- **백엔드**: `GET /api/comp/detail`, `POST /api/comp/update`, 목록에 `regNo` 포함, `MerchantProfile`에 bizType/industry/fax

### 3. 메뉴 정리
- **운영 메뉴 제거**: 업체관리 위의 사람 두 명 아이콘(운영) 플레이스홀더 삭제
- **리스크관리 추가**: 메뉴 최하단에 배치, 하위에 "리스크 현황" (`/risk/list`) 화면 정의 (API는 추후 연동)

### 4. 수정·추가된 파일 (참고)
- **프론트**: `site/index.html`, `site/login.html`, `site/js/app.js`, `site/js/screens.js`, `site/js/api.js`
- **백엔드**: `pg-app` — `ApiCompController`, `ApiHqController`, `CompService`, `MerchantProfile`, `HqApiConfig` 등

---

*이 요약은 2026-02-23 작업 기준입니다.*
