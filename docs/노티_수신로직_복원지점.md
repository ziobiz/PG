# 노티 수신 로직 복원 지점 (JPAY 원문 수신 / 303 리다이렉트 게이팅)

> 목적: 2026-06-19 에 추가한 **서버 릴레이 303 리다이렉트 방지(browserClient 게이팅)** 수정을,
> 신규 JPAY 결제 테스트가 실패할 경우 **정확히 그 부분만** 원래대로 복원하기 위한 기록.
> **URL 결제 표시옵션 기능은 절대 복원하지 않는다.** (사용자 명시 요청)

## 현재 상태 (유지 중)
- 조언대로 수정분을 **유지**하고, **신규 JPAY 결제 1건**으로 검증한다.
  (과거에 ChillPay 형태로 가공돼 쌓인 전문 재전송이 아니라, 신규 1건이 진짜 테스트)
- 검증 OK 면 이 문서는 무시. **검증 실패 시에만** 아래 복원 절차 수행.

## 복원 지점 (정확)
- 복원 대상 커밋: `5655ec1` — *"fix: JPAY 원문 노티 수신 처리 + 전산설정 금일 결제·노티 삭제 + URL결제 표시옵션"*
- 이 커밋에는 (A) 노티 수신 로직 + (B) 금일 결제·노티 삭제 도구 + (C) URL 결제 표시옵션 이 **함께** 들어있다.
- **"원론적인" 노티 수신 로직 변경은 단 한 파일에만 있다:**
  - `pg-app/src/main/java/com/pg/service/PgNotifyReceiveService.java`
    - 변경 내용: `shouldUsePayResultRedirect(...)` 에 `browserClient` 파라미터 추가 + `isLikelyBrowserClient(HttpServletRequest)` 추가.
    - 효과: form POST 등 서버-서버 릴레이는 303(`pay-result.html`) 대신 JSON(200/422)으로 응답.

## 복원 절차 (검증 실패 시에만)
이 파일 하나만 커밋 직전 상태로 되돌린다. (URL 결제 옵션·삭제도구는 전혀 영향 없음)

```bash
cd pg-app/..   # 저장소 루트(D:\Delopment\PG)
git checkout 5655ec1^ -- pg-app/src/main/java/com/pg/service/PgNotifyReceiveService.java
# 이후 반드시 재빌드
cd pg-app
./gradlew.bat clean bootJar --no-daemon
```

- 복원 후 산출 JAR: `pg-app/build/libs/pg-app-0.0.1-SNAPSHOT.jar` 업로드 + 서버 재시작.

## 절대 복원 금지 (URL 결제 표시옵션 — 유지)
아래 파일/변경은 **복원 대상이 아니다.**
- `pg-app/src/main/java/com/pg/urlpay/UrlPayPublicCheckoutService.java`
- `pg-app/src/main/java/com/pg/entity/MerchantProfile.java` (url pay 표시옵션 컬럼)
- `pg-app/src/main/java/com/pg/controller/api/ApiCompController.java` (url pay 옵션 저장)
- `pg-app/src/main/java/com/pg/service/CompService.java` (url pay 옵션)
- `pg-app/src/main/resources/db/V172_merchant_url_pay_presentation_options.sql`
- `site/js/url-pay-public-shell.js`, `site/pay.html`, `site/jpay-pay.html`
- `site/js/screens.js`, `app.js`, `api.js`, `pg-ui-i18n.js`, `pg-ui-string-map.js`, `site/index.html` 의 **URL 결제 옵션 관련 부분**

## 함께 유지 권장 (금일 결제·노티 삭제 도구)
복원 대상 아님(노티 수신 로직과 무관한 별도 유틸):
- `pg-app/.../controller/api/ApiHqLedgerSysSettingsController.java` (purge 엔드포인트)
- `pg-app/.../service/HqPayNotifyDayPurgeService.java`
- `pg-app/.../repository/PgNotifyInboundRepository.java`, `PgTrnsctnRepository.java`,
  `RollingReserveRepository.java`, `SettlementRecoveryRepository.java`
- 위 site UI 파일들의 **삭제 도구 관련 부분**
